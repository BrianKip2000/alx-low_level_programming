#include <stdio.h>

/**
 * main- main entry poiny
 * Return: returns 0 for all pass
*/

int main(void)
{
int i;

for (i = 48; i <= 57; ++i)
putchar(i);
for (i = 97; i <= 102; ++i)
putchar(i);/* code */
putchar(10);
return (0);
}
