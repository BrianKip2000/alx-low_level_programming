#include <stdio.h>

/**
 * main- main entry point
 * Return: returns 0 for correct checks
*/

int main(void)
{
int i;

for (i = 122; i >= 97; i--)
{
putchar(i);/* code */
}
putchar(10);
return (0);
}
