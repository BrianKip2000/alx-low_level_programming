#include <stdio.h>

/**
 * main- main entry point
 * Return: returns 0 if all checks are passed
*/

int main(void)
{
int i;

for (i = 0; i <= 9; i++)
{
printf("%d", i);/* prints output of for ftn */
}
return (0);
}
