#include <stdio.h>

/**
 * main- entry point
 * Return: return 0 of all checks are passed
*/

int main(void)
{
int n;

for (n = 97; n <= 122; n++)
{
putchar(n);
}
for (n = 65; i <= 90; n++)
{
putchar(n);
}
printf(n);
}
