#include <stdio.h>

/**
 * main- main entry point
 * Return: return 0 if all checks passed
*/

int main(void)
{
int i;

for (i = 0; i < 10; i++)
{
putchar(i);/* puchar changes to char */
}
putchar(10);
return (0);
}
